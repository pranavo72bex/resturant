import 'package:flutter/material.dart';

class ShoppingBagePage extends StatelessWidget {
  const ShoppingBagePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("ShoppingBagePage"),
      ),
    );
  }
}
